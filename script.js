// Toggle Navigation Menu
const hamburger = document.getElementById('hamburger');
const navLinks = document.getElementById('nav-links xxxxxxxxx');

hamburger.addEventListener('click', () => {
  navLinks.classList.toggle('active');
});
